<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.perez.studili">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_studili_logo"
        android:label="Studili"
        android:roundIcon="@mipmap/ic_launcher_studili_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CategoryActivity">
            <intent-filter>
                <action android:name="com.example.perez.studili.CategoryActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewTaskActivity"
            android:label="Create New Task"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AllNotificationActivity"
            android:label="My Notifications"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TaskDetailsActivity"
            android:label="@string/title_activity_task_details"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".GuessGameActivity"
            android:label="Number Guess Game"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>